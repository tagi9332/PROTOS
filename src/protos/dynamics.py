def propagate(config: dict):
    """Run the dynamics propagation based on config settings."""
    # Placeholder: replace with actual propagator
    trajectory = {"time": [0, 10, 20], "state": [[1,0,0], [0,1,0], [-1,0,0]]}
    return trajectory
